println : cfunc(format : string) -> null;
print	: cfunc(format : string) -> null;
println : cfunc(format : string) -> null;

Type : struct {
	field_int : int;
	field_ptr : int^;
};

main : () -> null {
	
	object : Type;

	object.field_int = 10;
	object.field_ptr = &object.field_int;
	object.field_int = ^object.field_ptr;
		
}
