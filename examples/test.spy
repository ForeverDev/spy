println : cfunc(format : string) -> null;
print	: cfunc(format : string) -> null;
println : cfunc(format : string) -> null;

Truck : struct {

};

Car : struct {
	field_int : Truck^;
};

main : () -> null {
	
	car : Car;
	truck : Truck;

	car.field_int = truck;

		
}
